# Every message id starts with this plugin's name, "hello-": the host refuses a
# plugin's locale file otherwise.
hello-plugin-name = hello
hello-plugin-cmd-greet = greet someone
hello-plugin-greetings = greetings so far: { $count }
